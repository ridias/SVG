let circle = document.getElementById("idCircle")

circle.addEventListener("click", (event) =>{
    console.log(event.target)
    //event.target.setAttribute("x", "10000")
    //console.log(event.target.attributes)
})